import React from "react";
import Modal from "../../../shared/components/modal/Modal";

const Reschedule = () => {
  return (
    <div className="task-reschedule-wrapper">
      <Modal isActive={true}>Are you sure</Modal>
    </div>
  );
};

export default Reschedule;
